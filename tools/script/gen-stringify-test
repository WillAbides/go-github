#!/bin/sh
#/ gen-stringify-test builds and runs tools/gen-stringify-test with the given arguments.

set -e

SCRIPT_DIR="$(CDPATH="" cd -- "$(dirname -- "$0")" && pwd -P)"
"$SCRIPT_DIR"/run-tool gen-stringify-test "$@"
