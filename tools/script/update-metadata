#!/bin/sh

set -e

TOOLS_DIR="$(CDPATH="" cd -- "$(dirname -- "$0")/.." && pwd -P)"

mkdir -p "$TOOLS_DIR"/bin

(
  cd "$TOOLS_DIR"
  go build -o bin/update-metadata ./update-metadata
)

exec "$TOOLS_DIR"/bin/update-metadata "$@"
